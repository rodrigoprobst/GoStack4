<html lang="en">

<head>
  <style>
    body {
      font-family: Arial, Helvetica, sans-serif
    }
  </style>
</head>

<body>
  <strong>Olá, {{ ad.author.name }}</strong>
  <p>Você tem uma nova solicitação de compra (<strong>{{ purchase._id }}</strong>) do anúncio {{ ad.title }}</p>
  <br>
  <strong>{{ user.name }} ({{ user.email }}): </strong>
  <p>{{ content }}</p>
</body>

</html>
